<form class="cyberpunk-blue sheet actor mook flexcol" autocomplete="off">
  <header class="sheet-header">
    <img class="profile-img" src="{{actor.img}}" data-edit="img" title="{{actor.name}}" />
    <div class="header-fields">
      <h1 class="charname">
        <input name="name" type="text" value="{{actor.name}}" placeholder="Mook Name" />
      </h1>
      <div class="header-stats">
        <label class="header-stat" title="Body">
          <span class="stat-short">BODY</span>
          <input type="number" name="system.stats.body.value" value="{{system.stats.body.value}}" data-dtype="Number" {{#unless isGM}}disabled{{/unless}} />
        </label>
        <label class="header-stat" title="Combat Number">
          <span class="stat-short">CN</span>
          <input type="number" name="system.combatNumber" value="{{system.combatNumber}}" data-dtype="Number" {{#unless isGM}}disabled{{/unless}} />
        </label>
        <div class="header-stat" title="Hit Points">
          <span class="stat-short">HP</span>
          <div class="resource-fields">
            <input type="number" name="system.resources.hp.value" value="{{system.resources.hp.value}}" data-dtype="Number" />
            <span>/</span>
            <input type="number" name="system.resources.hp.max" value="{{system.resources.hp.max}}" data-dtype="Number" {{#unless isGM}}disabled{{/unless}} />
          </div>
        </div>
        <div class="header-stat" title="Stopping Power (auto-derived from highest-SP armor item; set manually if no items)">
          <span class="stat-short">SP</span>
          <div class="resource-fields">
            <input type="number" name="system.resources.armor.value" value="{{system.resources.armor.value}}" data-dtype="Number" {{#unless isGM}}disabled{{/unless}} />
            <span>/</span>
            <input type="number" name="system.resources.armor.max" value="{{system.resources.armor.max}}" data-dtype="Number" {{#unless isGM}}disabled{{/unless}} />
          </div>
        </div>
        <label class="header-stat" title="Eurobucks">
          <span class="stat-short">€$</span>
          <input type="text" data-action="update-money" value="{{system.money}}" placeholder="0" title="Eurobucks — arithmetic OK (e.g. 500-50)" style="width:5rem;text-align:right;" />
        </label>
      </div>
    </div>
  </header>

  <nav class="sheet-tabs tabs" data-group="primary">
    <a class="item active" data-tab="basics"><span class="cpb-tab-icon" style="--cpb-icon: url('/systems/cyberpunk-blue/assets/icons/bk_Overview.svg')"></span>Basics</a>
    <a class="item" data-tab="items"><span class="cpb-tab-icon" style="--cpb-icon: url('/systems/cyberpunk-blue/assets/icons/bk_Inventory.svg')"></span>Items</a>
    {{#if isGM}}
      <a class="item" data-tab="notes"><span class="cpb-tab-icon" style="--cpb-icon: url('/systems/cyberpunk-blue/assets/icons/bk_Notes.svg')"></span>Notes</a>
    {{/if}}
  </nav>

  <section class="sheet-body">
    <div class="tab active" data-group="primary" data-tab="basics">
      <section class="sheet-panel">
        {{#if isGM}}
          <prose-mirror name="system.description" value="{{system.description}}" document-uuid="{{actor.uuid}}" compact>
            {{{enrichedDescription}}}
          </prose-mirror>
        {{else}}
          <div class="editor"><div class="editor-content">{{{enrichedDescription}}}</div></div>
        {{/if}}
      </section>

      <section class="sheet-panel">
        <div class="panel-header">
          <h2><span class="cpb-icon" style="--cpb-icon: url('/systems/cyberpunk-blue/assets/icons/bk_Skills.svg')"></span>Skills</h2>
          {{#if isGM}}
            <div class="add-row">
              <select data-skill-select>
                <option value="">— add skill —</option>
                {{#each allSkills as |sk|}}
                  <option value="{{sk.slug}}">{{sk.label}}</option>
                {{/each}}
              </select>
              <button type="button" data-action="add-skill" title="Add Skill">
                <i class="fas fa-plus"></i>
              </button>
            </div>
          {{/if}}
        </div>
        <div class="skill-list">
          {{#each system.skills as |skill i|}}
            <div class="skill-row">
              <span class="skill-label">{{skill.label}}</span>
              <input type="number" name="system.skills.{{i}}.rank" value="{{skill.rank}}" data-dtype="Number" min="0" />
              {{#if ../isGM}}
                <button type="button" data-action="remove-skill" data-skill-index="{{i}}" title="Remove">
                  <i class="fas fa-times"></i>
                </button>
              {{/if}}
            </div>
          {{/each}}
        </div>
      </section>

      <section class="sheet-panel">
        <div class="panel-header">
          <h2>Components</h2>
          {{#if isGM}}
            <div class="add-row">
              <select data-component-select>
                <option value="">— add component —</option>
                {{#each allComponents as |comp|}}
                  <option value="{{comp.slug}}">{{comp.label}}</option>
                {{/each}}
              </select>
              <button type="button" data-action="add-component" title="Add Component">
                <i class="fas fa-plus"></i>
              </button>
            </div>
          {{/if}}
        </div>
        <div class="skill-list">
          {{#each system.components as |comp i|}}
            <div class="skill-row">
              <span class="skill-label">{{comp.label}}</span>
              <input type="number" name="system.components.{{i}}.rank" value="{{comp.rank}}" data-dtype="Number" min="0" />
              {{#if ../isGM}}
                <button type="button" data-action="remove-component" data-component-index="{{i}}" title="Remove">
                  <i class="fas fa-times"></i>
                </button>
              {{/if}}
            </div>
          {{/each}}
        </div>
      </section>

      {{#if combatWeapons.length}}
        <section class="sheet-panel">
          <div class="panel-header"><h2>Weapons</h2></div>
          <table class="embedded-table">
            <thead>
              <tr>
                <th>Weapon</th>
                <th style="text-align:center;">Dmg</th>
                <th style="text-align:center;">RoF</th>
                <th style="text-align:center;">Ammo</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              {{#each combatWeapons as |w|}}
                <tr>
                  <td>{{w.label}}</td>
                  <td style="text-align:center;">{{w.damage}}</td>
                  <td style="text-align:center;">{{w.rof}}</td>
                  <td style="text-align:center;">{{#if w.usesMagazine}}{{w.ammoCurrent}}/{{w.ammoMax}}{{else}}—{{/if}}</td>
                  <td style="text-align:right;">
                    {{#if ../isGM}}
                      <button type="button" data-action="mook-weapon-attack" data-item-id="{{w.itemId}}" data-weapon-index="{{w.weaponIndex}}" title="Roll Attack">
                        <i class="fas fa-dice-d10"></i>
                      </button>
                    {{/if}}
                  </td>
                </tr>
              {{/each}}
            </tbody>
          </table>
        </section>
      {{/if}}

      {{#if martialArtsAttacks.length}}
        <section class="sheet-panel">
          <div class="panel-header"><h2>Martial Arts</h2></div>
          <div class="skill-list">
            {{#each martialArtsAttacks as |ma|}}
              <div class="skill-row">
                <span class="skill-label">{{ma.label}}</span>
                <span style="flex:0 0 auto;min-width:3rem;text-align:center;">{{ma.damage}}</span>
                {{#if ../isGM}}
                  <button type="button" data-action="mook-ma-attack" data-component-slug="{{ma.slug}}" title="Roll Attack">
                    <i class="fas fa-dice-d10"></i>
                  </button>
                {{/if}}
              </div>
            {{/each}}
          </div>
        </section>
      {{/if}}
    </div>

    <div class="tab" data-group="primary" data-tab="items">
      <section class="sheet-panel">
        <div class="items-list">
          {{#each embeddedItems as |item|}}
            <div class="item-row">
              <img src="{{item.img}}" title="{{item.name}}" class="item-img" />
              <span class="item-name">{{item.name}}</span>
              <span class="item-type">{{item.type}}</span>
              <button type="button" data-action="edit-item" data-item-id="{{item.id}}" title="Edit">
                <i class="fas fa-edit"></i>
              </button>
              {{#if ../isGM}}
                <button type="button" data-action="delete-item" data-item-id="{{item.id}}" title="Delete">
                  <i class="fas fa-trash"></i>
                </button>
              {{/if}}
            </div>
          {{/each}}
        </div>
      </section>
    </div>

    {{#if isGM}}
      <div class="tab" data-group="primary" data-tab="notes">
        <section class="sheet-panel">
          <prose-mirror name="system.notes" value="{{system.notes}}" document-uuid="{{actor.uuid}}" compact>
            {{{enrichedNotes}}}
          </prose-mirror>
        </section>
      </div>
    {{/if}}
  </section>
</form>
