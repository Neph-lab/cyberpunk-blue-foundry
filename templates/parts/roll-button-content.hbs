{{!-- Unified roll-button inner content: bk_d10 icon + signed modifier.
      Expects `mod` (e.g. "+5"). Put the breakdown on the button's data-tooltip. --}}
<span class="cpb-roll-d10" aria-hidden="true"></span><span class="cpb-roll-mod">{{mod}}</span>
